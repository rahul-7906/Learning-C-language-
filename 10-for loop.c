#include <stdio.h>

//For loop
int main(void) {
    for(int i=0;i<50;i=i+1){
    printf("Hello, World!\n");
    }
    // we can always use increment operator in place of i=i+1
    // increment operator i++
    // i++ (use then increase)
    //++i (increase then use)

    return 0;
}

